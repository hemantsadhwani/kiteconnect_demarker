## Deprecated

This file contained an old example (macOS paths + legacy workflow).

Please use:

- `docs/cron_usage.md`

It contains the **current EC2/Linux** instructions for:

- editing `crontab`
- verifying cron runs
- checking logs mid-day
- attach/detach via `tmux`